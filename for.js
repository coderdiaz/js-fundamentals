// For statement
var steps = 10;
for(var counter = 1; counter <= steps; counter++) {
  console.log('Step' + counter);
}

// Array iteration
var numbers = [0, 10, 15, 20, 15, 25];
for(var index = 0; index < numbers.length; index++) {
  console.log('Showed number is ' + numbers[index]);
}